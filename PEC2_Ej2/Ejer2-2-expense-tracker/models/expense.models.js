// models.js

// Clase de Transacción
class Transaction {
    constructor(id, text, amount) {
      this.id = id;
      this.text = text;
      this.amount = amount;
    }
  }
  
  export default Transaction;